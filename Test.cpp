#include <iostream>
#include <cstring>
using namespace std;

#define carl "yo"



type def Struct{
    char * Ville;
    bool Parcouru = false;
    char * Parent;
}Famille;

int main( Depart , Arrivee)
{
    Famille FamilleVilles[nbMaillon];
    int nbUniqueVille =0;
    int StartExplore = -1 ;

    const Maillon * DEBUT = Catalogue->GetDebut();

    Maillon * actuelle = DEBUT

    while( actuelle != nullptr)
        for i = 0 ; i < nbUniqueVille ; i++
            if ( actuelle->villedepart == FamilleVilles[i].Ville)
                existe = true;
        if existe != true
            FamilleVilles[ nbUniqueVille ].Ville = actuelle->villedepart
            
            if FamilleVilles[ nbUniqueVille ].Ville == Depart 
                StartExplore = nbUniqueVille
            
            nbUniqueVille++
        
        actuelle = actuelle->GetProchain();

    explore( FamilleVilles[StartExplore].Ville , FamilleVilles , nbUniqueVille , DEBUT)
}

void explore( const char * Ville , Famille * FamilleVilles , int nbUniqueVille , const Maillon * DebutListe)
{
    Maillon * actuelle = DebutListe

    

}